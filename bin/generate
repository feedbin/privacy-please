#!/usr/bin/env ruby

require "openssl"

url = ARGV.first.to_s

hex = url.to_enum(:each_byte).map { |byte| "%02x" % byte }.join

signature = OpenSSL::HMAC.hexdigest("sha1", "secret", url)

puts "/#{signature}/#{hex}"
